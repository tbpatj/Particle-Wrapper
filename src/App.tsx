import "./App.css";
import Page from "./Page";

function App() {
  return (
    <div className="App">
      <Page />
      {/* <input onChange={(e) => handleChange(e.target.value)} value={input} /> */}
    </div>
  );
}

export default App;
